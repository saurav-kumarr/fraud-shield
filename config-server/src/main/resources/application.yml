server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./config-server/src/main/resources/configs
    bus:
      enabled: true
    stream:
      kafka:
              binder:
                consumer-properties:
                  value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: "*"
